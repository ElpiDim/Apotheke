{
  "name": "@peanut/cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply peanut-staging --local",
    "db:migrate:remote": "wrangler d1 migrations apply peanut-staging --remote"
  },
  "dependencies": {
    "better-auth": "^1.7.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.40.0"
  }
}
